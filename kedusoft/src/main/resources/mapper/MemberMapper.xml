<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.home.kedusoft.mapper.MemberMapper">
	
	<select id="memberIdCheck" parameterType="memberDto" resultType="int">
		<![CDATA[	/*MemberMapper.memberIdCheck (회원가입 아이디 중복체크)*/
			SELECT COUNT(M_ID) AS CHECK_CNT
			  FROM MEMBER
			 WHERE 1 = 1
			   AND M_ID = #{memberId}
		]]>
	</select>
	
</mapper>